{
  "mcpServers": {
    "mcp-remote-test": {
      "command": "cargo",
      "args": [
        "run",
        "--bin",
        "mcp-remote",
        "--",
        "http://example.com/mcp",
        "--debug",
        "--allow-http"
      ],
      "env": {
        "RUST_LOG": "debug"
      }
    }
  }
}
